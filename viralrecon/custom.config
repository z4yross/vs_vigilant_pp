process {
    executor = 'k8s'
    k8s.pod = [
        [privileged = true],
        [volumeClaim: "claim-nextflow-data", mountPath: "/storage"]
    ]
}
k8s {
   storageClaimName = 'claim-nextflow-data'
   storageMountPath = '/storage'
}